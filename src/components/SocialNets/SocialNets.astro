---
import SocialNet from './components/SocialNet/SocialNet.astro'
import jsonData from '@/content/data.json'

const { socialNets } = jsonData
---

{
  socialNets.map(({ title, ...rest }) => (
    <SocialNet {...rest} title={`Ir al perfil de ${title}`} />
  ))
}
