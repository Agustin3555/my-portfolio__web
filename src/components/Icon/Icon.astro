---
import './Icon.css'

export interface Props {
  faIcon: string
  handlingClass?: string
}

const { faIcon, handlingClass } = Astro.props
---

<i class:list={['cmp-icon', handlingClass, faIcon]}></i>
