---
import './Separator.css'

export interface Props {
  asSpan?: boolean
  invert?: boolean
}

const { asSpan = false, invert = false } = Astro.props

const classList = ['cmp-separator', { invert }]
---

{asSpan ? <span class:list={classList} /> : <hr class:list={classList} />}
