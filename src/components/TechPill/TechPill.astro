---
import Pill from '../Pill/Pill.astro'

export interface Props {
  name: string
  version?: string
}

const { name, version } = Astro.props
---

<Pill size="s">
  {name}
  {version && <small>{version}</small>}
</Pill>
