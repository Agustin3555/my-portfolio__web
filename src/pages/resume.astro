---
import Page from '@/layouts/Page/Page.astro'
import BasicHeader from '@/layouts/BasicHeader/BasicHeader.astro'
import jsonData from '@/data.json'

const { pageTitle, data } = jsonData.pages.resume
---

<Page subtitle={pageTitle}>
  <BasicHeader {...data}>
    <embed
      class="cv"
      src="/CV - Juan Agustín Lovera - g_fswd_v1.pdf"
      type="application/pdf"
    />
  </BasicHeader>
</Page>

<style>
  .cv {
    aspect-ratio: 4 / 5;
    width: 100%;
    border-radius: var(--pal-size-font-xs);
    box-shadow: var(--decorator-shadow-1), var(--decorator-bevel-highlight);
  }
</style>
